# ============================================================================
# Frontend Environment Variables (.env.example)
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# ── Backend API URL ──
# Point to your deployed backend Azure App Service
# Development: http://localhost:3000/api/v1
# Production: https://your-backend.azurewebsites.net/api/v1
VITE_API_URL=https://your-backend.azurewebsites.net/api/v1

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# Vercel:
#   - Add environment variables in Project Settings > Environment Variables
#   - Variable must start with VITE_ to be exposed to client
#   - Will be embedded in build (public - don't put secrets here)
#
# Security:
#   - Never store secrets in frontend env vars
#   - All VITE_ variables are PUBLIC and embedded in JS bundle
#   - Sensitive operations (payments, auth) must happen on backend
# ============================================================================
